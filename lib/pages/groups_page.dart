import 'package:flutter/material.dart';

class Groups extends StatelessWidget {
  static const String routeName = "/groups_page";
  @override
  Widget build(BuildContext context) {
    return new Scaffold(
        body: new Container(
            child: new Center(
      child: new Text("Groupos"),
    )));
  }
}
