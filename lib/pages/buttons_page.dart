import 'package:flutter/material.dart';

class Buttons extends StatelessWidget {
  static const String routeName = "/buttons_page";
  @override
  Widget build(BuildContext context) {
    return new Scaffold(
        body: new Container(
            child: new Center(
      child: new Text("Buttones"),
    )));
  }
}
